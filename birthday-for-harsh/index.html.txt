<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Birthday My Love ❤️</title>

<style>
body {
    margin: 0;
    background: #000;
    color: #fff;
    font-family: Arial, sans-serif;
    overflow: hidden;
}
#text {
    position: absolute;
    top: 20%;
    width: 100%;
    text-align: center;
    font-size: 22px;
    line-height: 1.8;
}
canvas {
    display: block;
}
</style>
</head>

<body>

<div id="text">
    Hey Harshada… My Love 💞<br>
    Happy Birthday 🎈<br>
    May God bless you 🍀<br>
    Fill your life with endless happiness 💕<br>
    You are beautiful inside and out ❤️<br>
    You make my world brighter every day ✨<br>
    Forever yours 💕
</div>

<canvas id="canvas"></canvas>

<script>
const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");

canvas.width = window.innerWidth;
canvas.height = window.innerHeight;

let hearts = [];

function Heart() {
    this.x = Math.random() * canvas.width;
    this.y = canvas.height + Math.random() * 100;
    this.size = Math.random() * 15 + 10;
    this.speed = Math.random() * 1 + 0.5;
}

Heart.prototype.draw = function() {
    ctx.fillStyle = "pink";
    ctx.beginPath();
    ctx.moveTo(this.x, this.y);
    ctx.bezierCurveTo(this.x - this.size, this.y - this.size,
                      this.x - this.size * 2, this.y + this.size,
                      this.x, this.y + this.size * 2);
    ctx.bezierCurveTo(this.x + this.size * 2, this.y + this.size,
                      this.x + this.size, this.y - this.size,
                      this.x, this.y);
    ctx.fill();
};

Heart.prototype.update = function() {
    this.y -= this.speed;
};

function animate() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    if (hearts.length < 40) hearts.push(new Heart());
    hearts.forEach((h, i) => {
        h.update();
        h.draw();
        if (h.y < -50) hearts.splice(i, 1);
    });
    requestAnimationFrame(animate);
}

animate();
</script>

</body>
</html>
